{
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "scripts": {
    "start": "node app.js", // npm run start
    "compare": "node compare.js", // npm run compare
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
